# Secrets — never commit
.env
*.env
!.env.example

# Virtualenv / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Market data (large, regenerable)
data/cache/
data/instruments/*.csv
*.parquet

# Generated reports (published via soma-publish, not git)
reports/*.html
reports/*.png

# paper-trading runtime state — never in git
data/paper*.db
data/paper/
reports/daily/

# WS raw-frame captures (large, diagnostics-only) — keep the .json/.md verdicts
reports/ws_shadow/raw_frames_*.jsonl
reports/ws_shadow/raw_capture_*.jsonl
